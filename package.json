{
  "name": "kayjob-site",
  "version": "0.1.0",
  "private": true,
  "description": "Site web KayJob, marketplace nationale des talents étudiants au Sénégal.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "start": "npx serve public",
    "start:web": "npx serve apps/web",
    "start:api": "npm start --prefix apps/api",
    "api:release-expired": "npm run release-expired --prefix apps/api",
    "check": "node --check public/app.js && node --check apps/web/app.js && node --check apps/api/src/server.mjs && node --check apps/api/src/db.mjs && node --check apps/api/src/order-state.mjs && node --check apps/api/src/message-safety.mjs && node --check apps/api/src/release-expired.mjs && node --check scripts/neon-check.mjs && node --check scripts/neon-schema.mjs && node --check scripts/neon-migrate.mjs && node --check scripts/neon-tables.mjs",
    "db:check": "node scripts/neon-check.mjs",
    "db:schema": "node scripts/neon-schema.mjs",
    "db:migrate": "node scripts/neon-migrate.mjs",
    "db:tables": "node scripts/neon-tables.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1119.0",
    "@aws-sdk/s3-request-presigner": "^3.1119.0",
    "@neon/config": "^1.0.4",
    "@neon/env": "^1.1.4",
    "pg": "^8.23.0"
  }
}
